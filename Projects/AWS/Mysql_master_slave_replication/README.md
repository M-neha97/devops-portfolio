# MySQL Master–Slave Replication on AWS EC2

Implementation of MySQL 8.x Master–Slave replication on two Ubuntu EC2 instances inside the same AWS VPC.

This project demonstrates binary logging, replication configuration, and data synchronization between Master and Slave.


